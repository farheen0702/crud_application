Please clone the project using git clone <url>
    composer install / composer update
    create db : crud_application
    php artisan migrate
    php artisan serve : (http://127.0.0.1:8000/products)
